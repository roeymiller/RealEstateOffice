@model IEnumerable<RealEstateOffice.Models.Broker>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Deals)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Deals)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BrokerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BrokerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BrokerID })
        </td>
    </tr>
}

</table>
